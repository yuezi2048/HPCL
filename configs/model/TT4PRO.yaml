embedding_size: 64
feat_embed_dim: 64

n_mm_layers: 1
n_layers: 2
knn_k: 10
mm_image_weight: 0.1
learning_rate: [0.0001]
reg_weight: [0.0001]

dropout: [0.5]

lambda_1: [1.0] # CLUB 的互信息权重
lambda_graded_align: [0.5] # 原型对比学习的权重
alpha_g_s: [0.5]

#lambda_1: [0.1, 0.5, 1.0] # CLUB 的互信息权重
#lambda_graded_align: [0.2, 0.5, 0.8] # 原型对比学习的权重
#alpha_g_s: [0.3, 0.5, 0.7]

infoNCETemp: [0.4]


hyper_parameters: ["reg_weight", "learning_rate", 'dropout', 'mask_weight_f', 'lambda_1',
                   'mask_weight_g', 'infoNCETemp', "lambda_graded_align",  "alpha_g_s"]